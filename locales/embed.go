// Package locales хранит переводы интерфейса и встраивает их в бинарник.
//
// Файлы лежат отдельно от кода намеренно: переводы правят руками, иногда
// не программисты, и им незачем разбираться в устройстве адаптера.
// Встраивание через embed.FS означает, что образу не нужны файлы рядом —
// бинарник самодостаточен, как и с миграциями.
package locales

import "embed"

// FS содержит все файлы переводов.
//
//go:embed *.toml
var FS embed.FS
