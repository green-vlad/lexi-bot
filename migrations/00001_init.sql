-- Первая миграция намеренно ничего не меняет: она проверяет, что механизм
-- накатывания и отката работает от начала и до конца. Реальная схема появится
-- в отдельной миграции (задача T-012).

-- +goose Up
SELECT 1;

-- +goose Down
SELECT 1;
