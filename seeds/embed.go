// Package seeds хранит встроенные словари и встраивает их в бинарник.
//
// Файлы лежат отдельно от кода намеренно: словари правят руками, часто
// не программисты, и им незачем разбираться в устройстве загрузчика.
// Встраивание через embed.FS означает, что сидеру не нужны файлы рядом —
// он самодостаточен, как и миграции с локалями.
package seeds

import "embed"

// FS содержит манифест колод и файлы со словами.
//
//go:embed decks.yaml decks/*.csv
var FS embed.FS
